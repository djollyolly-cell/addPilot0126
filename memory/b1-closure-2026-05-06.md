# B1 closure — `getAccountAllAdIds` historical metrics timeout

Date closed: 2026-05-06
Reconstructed/updated: 2026-05-12; 7-day closeout recorded 2026-05-13
Status: **closed — function-level verified; 7-day organic observation window closed clean 2026-05-13T19:55:59Z (post-cutoff 19:53:27Z); no B1 rollback signal**
Merge-readiness note: this closes the B1 bug-fix closure AND the separate `>=1 week observation` merge-readiness checklist item. Post-window read-only recheck was completed at `2026-05-13T19:55:59Z`; see "Organic E2E read-only verification (2026-05-13) — 7-day window closed" section below.
Supersedes: `memory/todo-getAccountAllAdIds-pagination.md` (resolved by this fix)
Reference: `docs/2026-05-06-getAccountAllAdIds-fix-design.md` (Tier 1 / Option A)

## Summary

B1 = Tier 1 / Option A fix for `ruleEngine.getAccountAllAdIds` historical-metrics timeout. Replaced unbounded `metricsDaily` collection with `ads.by_accountId_vkAdId.collect()`. Function returns currently-known vkAdIds for an account, bounded by current ad count instead of historical metrics rows.

Pre-existing production correctness bug (rule evaluation silently failed for heavy accounts on `cpl_limit since_launch` and other non-`daily`-window rule types). Pre-dated the 2026-05-04/05 incident; surfaced by RCA during recovery.

## Deploy

- Commit: `9768449 fix(rules): query ads table for getAccountAllAdIds`
- Pushed: `origin/emergency/drain-scheduled-jobs` (current deployment source per merge-cleanup-scope Strategy C)
- Deployed: `2026-05-06T19:53:27Z` via `npx convex deploy --yes` against `http://178.172.235.49:3220` with admin key from `gen-admin-key.cjs`. Deploy returned clean (no schema deletes, schema validation passed).
- GitHub `Deploy` workflow not used — only triggers on push to `main`. Manual Convex deploy is the documented mechanism for this branch (per `merge-cleanup-scope.md:157` and `CLAUDE.md` deploy section).

## Verification levels

| Layer | Result |
|---|---|
| `npx tsc --noEmit -p convex/tsconfig.json` | clean |
| `npm run test` (full suite) | 711 passed; 2 pre-existing failures in `convex/vkApiLimits.test.ts > recordRateLimit` confirmed unrelated by stash-and-rerun on `506c500` (pre-B1). Out of B1 scope; will be addressed by D2a. |
| New unit tests in `convex/ruleEngine.test.ts > getAccountAllAdIds` | 2 tests pass: returns expected vkAdId set from `ads`; returns empty for account with no ads. Verified historical-only adIds in `metricsDaily` are NOT returned. |
| Pre-impl gate (read-only) | `j978z1sbh3ra5ym2hh3wqmb88184cs47` `ads`=1327 (matches design-doc); `j974v8cpc3zg8tk07maqs39ejh842fz2` `ads`=1136 (matches design-doc). |
| Function-level prod call (post-deploy, direct admin API to `internal.ruleEngine.getAccountAllAdIds`) | Вардек 1327 ads / 1160ms; Интерьер 1136 ads / 1080ms. Round-trip includes TLS + admin auth + remote-network latency. Server-side query time is a fraction of that. Compare to old impl: timeout (>10s). |
| Function-level prod call (re-run 2026-05-12, direct admin API to `internal.ruleEngine.getAccountAllAdIds`) | Вардек 1334 ads / 1247ms; Интерьер 1144 ads / 853ms. Counts vs 2026-05-06 baseline (Вардек 1327, Интерьер 1136): `+7` and `+8` ads, consistent with natural inventory growth over 6 days. Latency same order as initial verification, well below pre-B1 timeout (>10s). One first-pass request hit transient network noise (Вардек fetch failure; Интерьер 6933ms outlier); the clean second-pass run above is the authoritative verification sample. |
| `/version` HTTP 200 | Both DNS (`https://convex.aipilot.by/version`) and direct (`http://178.172.235.49:3220/version`) return 200. Body returns `unknown` for self-hosted Convex (expected). |
| Organic E2E (rule check via `syncBatchWorkerV2` for affected accounts) | **Deferred — not a B1 issue.** See below. |

## Why organic E2E is deferred

53 minutes after deploy, neither affected account had received an organic sync tick:

- Вардек `lastSyncAt`: `2026-05-06T12:19:56Z` (507 min ago at watcher kill)
- Интерьер `lastSyncAt`: `2026-05-06T10:49:17Z` (597 min ago at watcher kill)
- System-wide sync coverage: `9 / 212` active accounts synced in last 20 min
- `cronHeartbeats.syncDispatch`: `отстаёт 13 мин`
- Both accounts: `status=active`, fresh tokens, `consecutiveSyncErrors=0`, `lastError=null`

Phase 6 conservative profile runs `syncDispatch` every 45 min processing a small subset of accounts per tick. With 212 active accounts and ~9 acc / 20-min, full coverage takes ~16h. Heavy accounts at the back of dispatcher queue can wait many hours for their turn.

This is **environment throughput throttling**, not a B1 correctness regression. Function-level verification proves the fix works end-to-end inside `internal.ruleEngine.getAccountAllAdIds`; the organic-tick path on the watched accounts is the next dependent step but cannot be observed on hour scale under Phase 6.

Direct-invocation E2E (calling `internal.ruleEngine.checkRulesForAccount`) was rejected because `checkRulesForAccount` has real `stopAd` / notify side effects and is gated behind business approval, not just operator approval. Per UZ-style policy: side-effecting automation only with explicit business go.

## Watcher

`check-b1-watch.cjs` polled `lastSyncAt` for both accounts every 60s, expected exit when both advanced past deploy timestamp. Killed manually at `2026-05-06T20:46Z` after 53 min without advancement. Watcher behavior was correct; environment did not produce a tick within the window.

## Acceptance signal that was watched (24-48h post-deploy)

*Note (2026-05-12): the speculative framing below was superseded by the organic E2E read-only verification section further down; kept here as a historical record of the post-deploy plan.*

What was watched on the two affected accounts (`j978z1sbh3ra5ym2hh3wqmb88184cs47`, `j974v8cpc3zg8tk07maqs39ejh842fz2`):

- `actionLogs` for new `cpl_limit since_launch` triggers (`stopped` / `notified`). Any such entry would have confirmed rules now enforce.
- `systemLogs` for new `checkRulesForAccount failed: request timed out` entries. Expected: zero.

What would have counted at the time of writing: zero of the second list within 48h. That framing was overtaken by the 2026-05-12 verification below, which provides direct organic evidence rather than speculative absence.

### Post-window status (2026-05-12 doc reconciliation)

*Update: see "Organic E2E read-only verification (2026-05-12)" below — live read-only verification was executed after this reconciliation note was written.*

The 24-48h observation window has elapsed, and later sync-throughput follow-ups (`SYNC_BATCH_SIZE_V2` `10 -> 20`, `SYNC_WORKER_COUNT_V2` `1 -> 2`) closed clean, so the two known affected accounts likely had opportunity to receive organic sync coverage.

However, no canonical read-only verification query is recorded in memory for the acceptance signal above. This doc-only reconciliation did **not** run live Convex admin queries. Treat B1 as **landed and function-level verified**, with organic E2E / one-week observation still pending a separate read-only verification session.

## Organic E2E read-only verification (2026-05-12)

Window: `2026-05-06T19:53:27Z` (`1778097207000`) → `2026-05-12` (verification run on day 6 of 7-day observation window; window closes `2026-05-13T19:53:27Z`).

Verdict: **organic absence-of-regression evidence is positive; full organic positive-trigger E2E remains inconclusive.**

Scope note: this section documents day-6 absence-of-regression evidence. It does **not** complete the separate merge-readiness requirement for `>=1 week observation`; run a small read-only recheck after `2026-05-13T19:53:27Z` before changing the merge checklist checkbox.

### Results

| Query | Result |
|---|---|
| C — Account coverage | Both targets `status=active` and synced after B1 deploy: Вардек `lastSyncAt=2026-05-12T05:34:24.483Z`; Интерьер `lastSyncAt=2026-05-12T06:04:27.151Z`. Both `consecutiveSyncErrors=0`, `lastSyncError=(none)`, `lastError=(none)` — no account-level error state on either target as of verification time. Source: `check-b1-cron.cjs`. |
| D — Current relevant rules | Вардек: 1 active rule (`kd79bs1wbvbyk9dezjnn3j5nds84n36r`, `type=cpl_limit`, `timeWindow=since_launch`, `stopAd=true`, `notify=false`). Интерьер: 0 current active relevant rules. |
| A — Timeout recurrence | No `checkRulesForAccount failed` + `request timed out` matches found in the read-only `systemLogs` sample checked on 2026-05-12 for either target across the post-deploy window. Sample coverage was reported as full for the window. |
| B — Positive action trigger | Targeted by-ruleId query for Вардек's relevant rule: `26` historical logs, latest `2026-05-02T18:50:24.343Z` (4 days before B1 deploy), `0` post-deploy. Broad `actionLogs` CLI sweep was sample-limited (default-paged from latest) and did not cover the full window; targeted by-ruleId query is authoritative. |

### Interpretation

The strongest signal is absence of regression. Per `memory/todo-getAccountAllAdIds-pagination.md:67-72`, both targets historically had `4` `checkRulesForAccount timed out` records each in the 10-day pre-incident window. Post-B1, the 2026-05-12 read-only verification found no such records for the same accounts in the post-deploy window, while organic sync coverage was confirmed and account-level error state was clean. Historical `4` per account → `0` observed in this sample is concrete absence-of-regression evidence.

Positive-trigger proof is unavailable. Вардек's relevant rule was already quiet for ~4 days before B1 deploy (latest action log `2026-05-02`, deploy `2026-05-06`), so `0` post-deploy actions cannot be attributed to B1 — the rule was not firing in the days leading up to the fix either. Интерьер no longer has a current active relevant `cpl_limit since_launch` rule, so it cannot produce positive-trigger evidence under current configuration.

Rule-configuration drift since the 2026-05-06 RCA: Вардек had 2 relevant rules then ("ВАРДЕК подп сенл спб до 600 р", "...мск до 1200 р"), 1 now; Интерьер was affected by similar timeouts then, has 0 relevant rules now. Both accounts belong to one user (`userId=kx7dz8f32fv6b4psr9skscpqtd8431rx`, Наталия Галимханова, `pro` tier), so the drift is one operator's configuration evolution, not two independent signals.

Methodology note: the broad `actionLogs` CLI sweep was sample-limited and did not cover the full B1 window. The targeted `by_ruleId` index query for the relevant Вардек rule covered all `26` historical entries for that rule and is the authoritative source for the Query B result.

Conclusion: B1 remains **landed and function-level verified**, with **organic absence-of-regression observed** in the read-only sample. Full organic positive-trigger E2E is **inconclusive**, not failed.

## Organic E2E read-only verification (2026-05-13) — 7-day window closed

Window: full 7 days from `2026-05-06T19:53:27Z` (B1 deploy) to `2026-05-13T19:53:27Z` (cutoff). Verification run started at `2026-05-13T19:55:59Z` (post-cutoff, `+2m32s`). All probes read-only via Convex admin path; no env, code, schema, or deploy changes.

Verdict: **PASS — full-window absence-of-regression confirmed**. Positive-trigger E2E remains **inconclusive** per the 2026-05-12 caveat (rule-configuration drift unchanged).

### Results

| Probe | Result |
|---|---|
| `/version` | HTTP 200 |
| Account coverage — Вардек | `lastSyncAt=2026-05-13T18:34:26Z`, `consecutiveSyncErrors=0`, no `lastSyncError`, no `lastError` |
| Account coverage — Интерьер | `lastSyncAt=2026-05-13T18:49:28Z`, `consecutiveSyncErrors=0`, no `lastSyncError`, no `lastError` |
| Function-level direct call — Вардек | `1334` ads, `1069 ms` (vs 2026-05-06 baseline `1327 ads`, `+7` natural growth; vs 2026-05-12 sample `1334 ads / 1247 ms`, same ad count, latency within sample variance) |
| Function-level direct call — Интерьер | `1144` ads, `1038 ms` (vs 2026-05-06 baseline `1136 ads`, `+8` natural growth; vs 2026-05-12 sample `1144 ads / 853 ms`, same ad count) |
| `systemLogs` errors since B1 deploy (`2026-05-06T19:53:27Z`) — full window | **12 rows total** — 10 controlled (`d1a-gate-test` ×5 from 2026-05-12 D1a verification + `d1b-gate-test` ×5 from 2026-05-13 D1b verification) + 2 unrelated `auth` concurrency rows from `2026-05-10` (post-Phase-8 organic, not B1 pattern) |
| `checkRulesForAccount` failed / `request timed out` patterns in full window | **0** — canonical B1 regression signature absent |

### Interpretation

Per `memory/todo-getAccountAllAdIds-pagination.md:67-72`, both target accounts each had `4` `checkRulesForAccount timed out` records in the 10-day pre-incident window. The 7-day post-B1 window shows `0` such records for the same accounts while organic sync coverage remained continuous and account-level error state stayed clean throughout. Historical `4 + 4 → 0` is concrete absence-of-regression evidence over the full observation window.

Background noise in the systemLogs scan is fully attributable: `d1a-gate-test` and `d1b-gate-test` rows are controlled D1 verification bursts (filter source going forward, same pattern as `fanout-gate-test`); the 2 `auth` concurrency rows from `2026-05-10` predate D1a deploy and are unrelated to B1.

Positive-trigger E2E remains **inconclusive**, not failed, for the same reasons recorded in the 2026-05-12 section: Вардек's relevant rule was already quiet pre-deploy; Интерьер no longer has a current active relevant `cpl_limit since_launch` rule. Rule-configuration drift is unchanged since the 2026-05-12 sample, so positive-trigger evidence cannot be produced under current configuration — this is a configuration condition, not a B1 fault, and does not gate the absence-of-regression closure.

Conclusion: 7-day organic E2E observation window **closed clean**. `>=1 week observation confirmed by read-only query` merge-readiness criterion is now satisfied for marking the corresponding checkbox in `docs/2026-05-06-merge-cleanup-scope.md`.

## Follow-up triggers (separate decisions, not auto-implied by B1)

### F1. Sync throughput bump

Phase 6 throughput was the bottleneck for B1 acceptance verification AND for user-visible rule freshness. With 212 active accounts and 16h full coverage, rules lagged actual ad performance by hours.

Candidate parameters (each required own design, acceptance, rollback):

- `SYNC_BATCH_SIZE_V2` 10 → 20 (first env-only candidate; raising to 30 requires a code change to lift the in-code clamp and is a separate track). **Closed clean 2026-05-06** per `memory/phase-6-sync-canary-status.md`.
- `SYNC_WORKER_COUNT_V2` 1 → 2 (doubles throughput, doubles concurrent V8 slot usage). **Closed clean 2026-05-07** per `memory/phase-6-sync-canary-status.md`.

Constraints:

- `APPLICATION_MAX_CONCURRENT_V8_ACTIONS=16` ceiling.
- Phase 8 reservation: 50% of V8 slots reserved for non-fan-out work (crons, rule checks, token refresh, user queries).
- Per-account heavy-ad outlier inventory (Вардек 1327 / Интерьер 1136 known; new outliers may have emerged).
- Rollback: env-only revert.
- Acceptance: at least one full sync coverage cycle observed.

Do NOT bundle with B1.

### F2. JSDoc / rename `getAccountAllAdIds`

Function name implies "all historical adIds". After B1 it returns "currently-known adIds for the account". Function signature unchanged so no caller churn. Implementation hint in design doc left this as advisory. Possible name: `getAccountAdIds`. Cosmetic, low priority.

## Files touched

- `convex/ruleEngine.ts:646-660`: function body swapped (5 meaningful LOC).
- `convex/ruleEngine.test.ts`: +131 LOC across 2 new tests under `describe("getAccountAllAdIds")` + import added for `internal`.

Diff: `+135 / -10` total.

## Rollback

If F1-style sync throughput bump or future change reveals B1 missed an edge case:

```
git revert 9768449
git push origin emergency/drain-scheduled-jobs
CONVEX_SELF_HOSTED_URL=http://178.172.235.49:3220 \
CONVEX_SELF_HOSTED_ADMIN_KEY="$(node gen-admin-key.cjs)" \
npx convex deploy --yes
```

Code rollback + deploy. No data migration. No schema change. Reversible end-to-end.

## One-line judgment

**B1 closed:** `getAccountAllAdIds` timeout resolved at function level; **7-day organic observation window closed clean 2026-05-13T19:55:59Z** (both affected accounts synced continuously across full window with no `checkRulesForAccount timed out` recurrences and no account-level error state). Positive-trigger E2E remains **inconclusive** — current rule-configuration cannot produce that evidence (Вардек's relevant rule was quiet pre-deploy; Интерьер has no current active relevant rule); this is a configuration condition, not a B1 fault, and does not gate the absence-of-regression closure. Stopped-ad action triggers depend on rule configuration and ad performance, and are out of scope for this closure.
