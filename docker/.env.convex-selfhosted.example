# ============================================
# AdPilot Self-Hosted Convex — Environment Variables
# Задать в Dokploy Environment для compose-сервиса
# ============================================

# Уникальное имя инстанса
INSTANCE_NAME=adpilot-prod

# Секрет инстанса (сгенерировать: openssl rand -hex 32)
INSTANCE_SECRET=

# Пароль PostgreSQL (сгенерировать: openssl rand -hex 16)
POSTGRES_PASSWORD=
