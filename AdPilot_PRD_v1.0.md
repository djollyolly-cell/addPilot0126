# AdPilot — Product Requirements Document

**Версия:** 1.0  
**Статус:** Draft  
**Дата:** 20 января 2026 г.  
**Автор:** Product Team

---

## Содержание

1. [Информация о документе](#1-информация-о-документе)
2. [Executive Summary](#2-executive-summary)
3. [Целевая аудитория](#3-целевая-аудитория)
4. [Функциональные требования](#4-функциональные-требования)
5. [Описание интерфейса](#5-описание-интерфейса)
6. [Архитектура системы](#6-архитектура-системы)
7. [Технические требования](#7-технические-требования)
8. [Матрица RACI](#8-матрица-raci)
9. [Дорожная карта](#9-дорожная-карта)
10. [Риски и митигация](#10-риски-и-митигация)
11. [Метрики успеха](#11-метрики-успеха)
12. [Приложения](#12-приложения)

---

## 1. Информация о документе

### Стейкхолдеры

| Роль | Ответственность |
|------|-----------------|
| CEO | Стратегическое видение, финальное утверждение |
| CTO | Техническая архитектура, выбор стека |
| Tech Lead | Реализация, код-ревью, технические решения |
| Design Lead | UX/UI, пользовательские сценарии |
| Marketing Lead | Позиционирование, GTM-стратегия |

### История изменений

| Версия | Дата | Автор | Изменения |
|--------|------|-------|-----------|
| 0.1 | 15.01.2026 | PM | Первичный драфт |
| 0.5 | 18.01.2026 | PM + Tech Lead | Добавлена архитектура |
| 1.0 | 20.01.2026 | Team | Финальная версия для review |

---

## 2. Executive Summary

### Миссия продукта

**AdPilot** — автоматизированный ассистент для таргетологов VK Ads, предотвращающий неэффективный расход рекламного бюджета через мониторинг в реальном времени и автоматические действия.

### Ключевая проблема

Таргетологи физически не могут контролировать рекламные кампании 24/7. Это приводит к:
- «Сливу» бюджета ночью и в выходные
- Потере денег на неэффективных объявлениях
- Репутационным рискам перед клиентами
- Стрессу и профессиональному выгоранию

### Решение

Автоматизированная система правил с мгновенными уведомлениями в Telegram и прозрачной визуализацией сэкономленных средств.

### Ключевое сообщение (USP)

> **«AdPilot сохраняет ваш бюджет, пока вы спите»**

### Целевые показатели MVP

| Метрика | Целевое значение |
|---------|------------------|
| Retention Rate (1 месяц) | > 40% |
| Снижение CPL (тестовая группа) | > 15% |
| Конверсия в платную подписку | > 5% |
| Стабильность API (beta) | 14 дней без критических сбоев |

---

## 3. Целевая аудитория

### 3.1. Первичная: Фрилансеры-таргетологи

**Характеристики:**
- Ведут 3-15 клиентов одновременно
- Высокий репутационный риск при ошибках
- Необходимость демонстрировать ценность клиентам
- Часто работают с ограниченными бюджетами

**Боли:**
- Невозможность контролировать кампании ночью
- Страх «проснуться и увидеть слитый бюджет»
- Сложность доказать клиенту свою эффективность

**Потребности:**
- Автоматическая защита бюджета
- Уведомления о критичных событиях
- Отчёты для демонстрации клиентам

### 3.2. Вторичная: Инхаус-маркетологи

**Характеристики:**
- Работают в штате компании
- Отчитываются перед руководством
- Бюджеты обычно выше, чем у фрилансеров

**Боли:**
- Ответственность за бюджет перед руководством
- Страх ночных «сливов» при спящем бюджете

### 3.3. Третичная: Небольшие агентства (2-5 человек)

**Характеристики:**
- Масштабирование без потери качества
- Нужна операционная эффективность
- Работа с множеством клиентских кабинетов

---

## 4. Функциональные требования

### 4.1. Модуль автоматических правил

#### Типы правил

| Правило | Описание | Параметры | Действие по умолчанию |
|---------|----------|-----------|----------------------|
| Лимит CPL | Остановка при превышении стоимости лида | Порог в ₽, мин. кол-во лидов | Остановить + уведомить |
| Минимальный CTR | Остановка при низкой кликабельности | Порог в %, мин. показы | Уведомить |
| Быстрый скрут | Детекция аномального расхода за 15 мин | % от дневного бюджета | Остановить + уведомить |
| Сумма без лидов | Остановка при расходе без конверсий | Макс. сумма в ₽ | Остановить + уведомить |

#### Требования к движку правил

- Частота проверки: каждые 5 минут
- Время реакции: < 60 секунд после обнаружения
- Полное логирование всех срабатываний
- Возможность отката действия в течение 5 минут

### 4.2. Система уведомлений

#### Каналы

**Основной канал:** Telegram (нативная среда для целевой аудитории)

#### Типы уведомлений

| Тип | Приоритет | Доставка |
|-----|-----------|----------|
| Критические | Высший | Мгновенно, всегда |
| Стандартные | Средний | Группировка по 5 минут |
| Дайджест | Низкий | Ежедневно в 09:00 |

#### Формат уведомления

```
🚨 Правило сработало: Лимит CPL

📊 Кампания: "Продажа курсов"
📢 Объявление: "Скидка 50% на обучение"
❌ Причина: CPL (650 ₽) > порог (500 ₽)
💰 Потенциальная экономия: ~2,340 ₽

[Отменить остановку] [Подробнее]
```

### 4.3. Дашборд «Сэкономлено»

#### Отображаемые метрики

- Сэкономлено сегодня / за неделю / за месяц (₽)
- Количество срабатываний правил
- Топ-3 остановленных объявления
- ROI сервиса (сэкономлено / стоимость подписки)

#### Алгоритм расчёта экономии

```
Потенциальная экономия = 
  (средний расход в минуту за последний час) × 
  (оставшееся время до конца дня)
```

### 4.4. Авторизация в системе

#### Способы входа

| Способ | Описание | Примечание |
|--------|----------|------------|
| VK OAuth | Авторизация через аккаунт ВКонтакте | Основной способ, даёт доступ к VK Ads API |
| Email + пароль | Вход через почту VK Ads кабинета | Альтернативный способ для пользователей, предпочитающих вход по почте |

#### Вход через Email (VK Ads)

1. Пользователь вводит email и пароль от кабинета VK Ads
2. Система выполняет аутентификацию через VK Ads
3. При успехе — создание/обновление сессии, переход в кабинет
4. При ошибке — отображение сообщения с причиной (неверный email/пароль, аккаунт заблокирован)

#### Требования к форме входа по Email

- Поле email с валидацией формата
- Поле пароля с маской ввода и кнопкой «показать/скрыть»
- Кнопка «Войти»
- Ссылка «Войти через VK» для переключения на OAuth
- Обработка ошибок: неверные данные, сетевые ошибки, блокировка аккаунта
- Rate limiting: максимум 5 попыток за 15 минут

### 4.5. Подключение рекламного кабинета

#### Процесс подключения

1. Авторизация через VK OAuth или Email
2. Получение списка доступных кабинетов
3. Выбор кабинета для мониторинга
4. Синхронизация кампаний и объявлений
5. Применение пресета правил или кастомная настройка

#### Лимиты по тарифам

| Тариф | Кабинеты | Правила | Автоостановка |
|-------|----------|---------|---------------|
| Freemium | 1 | 2 | ❌ |
| Start | 3 | 10 | ✅ |
| Pro | ∞ | ∞ | ✅ |

---

## 5. Описание интерфейса

### 5.1. Главный дашборд

#### Центральный виджет «Сэкономлено»

- Крупная анимированная цифра в рублях с обновлением в реальном времени
- Мини-график за последние 7 дней под основной цифрой
- Процент изменения относительно предыдущего периода

#### Блок активности

- Срабатываний сегодня (число + тренд)
- Остановлено объявлений (число)
- Отправлено уведомлений (число)
- Каждая метрика кликабельна для перехода к детальному логу

#### Карточки кабинетов

- Горизонтальный список карточек подключённых кабинетов
- На каждой карточке: название, статус синхронизации, активных правил, расход сегодня
- Индикатор «здоровья»: зелёный/жёлтый/красный

#### Лента событий

- Хронологический список последних событий
- Формат: время, тип события, краткое описание, кнопка «Подробнее»
- Фильтры по типу и кабинету

### 5.2. Экран настройки правил

#### Левая колонка: Список правил

- Карточки существующих правил
- На карточке: название, переключатель вкл/выкл, статистика срабатываний, привязанные кабинеты
- Кнопка «+ Новое правило»

#### Правая колонка: Редактор правила

**Конструктор условия:**
- Выпадающий список метрики (CPL, CTR, Расход, Показы)
- Оператор сравнения (>, <, =, ≥, ≤)
- Поле ввода значения с валидацией

**Блок действий:**
- Радио-кнопки: Остановить / Только уведомить / Остановить и уведомить
- Настройка канала уведомления
- Задержка перед действием (опционально)
- Кастомный текст уведомления

**Селектор применения:**
- Древовидная структура: Кабинет → Кампания → Объявление
- Чекбоксы для множественного выбора
- Возможность выбрать уровень (все объявления кампании)

### 5.3. Экран аналитики

#### Верхняя панель

- Селектор периода (7/30/90 дней, произвольный)
- Фильтр по кабинетам

#### Блок 1: Экономия

- График экономии по дням (линейный)
- Разбивка по типам правил (столбчатая)
- Сравнение с предыдущим периодом

#### Блок 2: Срабатывания

- Круговая диаграмма по типам правил
- Топ-10 объявлений с наибольшим количеством остановок
- Тренд срабатываний

#### Блок 3: ROI

- Расчёт окупаемости сервиса
- Прогноз экономии на следующий период

#### Экспорт

- PNG для графиков
- CSV для таблиц

### 5.4. Экран логов

#### Лента событий

- Хронологический список всех событий
- Временная метка с точностью до секунды
- Тип события (срабатывание, остановка, уведомление, ошибка)
- Затронутый объект (кабинет/кампания/объявление)
- Детали действия
- Статус (успешно/ошибка/отменено)

#### Фильтры

- По типу события
- По кабинету
- По статусу
- По дате
- Полнотекстовый поиск

#### Детальная панель

- При клике на событие открывается боковая панель
- Показывает: метрики на момент срабатывания, применённое правило, скриншот объявления (если есть)
- Кнопка «Отменить действие» (если применимо)

### 5.5. Настройки

#### Профиль

- Данные пользователя
- Смена пароля
- Управление подпиской
- История платежей

#### Telegram

- Инструкция по подключению бота
- QR-код для быстрого подключения
- Настройки уведомлений
- Тихие часы

#### API

- Статус подключения к VK Ads
- Лог ошибок синхронизации
- Кнопка принудительной синхронизации
- Настройка частоты обновления

### 5.6. Мобильная адаптация

#### Компоновка

- Вертикальный стек карточек
- Виджет экономии → Кабинеты → Лента событий

#### Навигация

- Нижнее меню: Главная, Правила, Аналитика, Логи, Настройки

#### Особенности

- PWA с push-уведомлениями
- Оффлайн-режим для просмотра последних данных
- Дублирование критичных уведомлений в Telegram

---

## 6. Архитектура системы

### 6.1. Компоненты системы (Convex + React)

```
┌─────────────────────────────────────────────────────────────────┐
│                    REACT APPLICATION                            │
│                   (Vite + React 18+)                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  Dashboard  │  │   Rules     │  │  Analytics  │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  useQuery() ──── Real-time subscriptions ──── useMutation()    │
└────────────────────────────┬────────────────────────────────────┘
                             │ WebSocket (автоматически)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                         CONVEX                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Convex Functions                      │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │   │
│  │  │ Queries  │  │Mutations │  │ Actions  │  │  Crons   │ │   │
│  │  │(read DB) │  │(write DB)│  │(HTTP/AI) │  │(scheduled│ │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Convex Database (Document DB)               │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │   │
│  │  │ users  │ │accounts│ │ rules  │ │ logs   │ │metrics │ │   │
│  │  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │  VK Ads API │    │  Telegram   │    │   OpenAI    │
  │  (Actions)  │    │  Bot API    │    │   (AI)      │
  └─────────────┘    └─────────────┘    └─────────────┘
```

### 6.2. Потоки данных

#### Поток синхронизации метрик

```
1. Convex Cron запускает syncMetrics каждые 5 минут
2. Action вызывает VK Ads API (ads.getStatistics)
3. Mutation записывает данные в Convex Database
4. Все подписанные клиенты получают обновления автоматически (real-time)
```

#### Поток проверки правил

```
1. После записи метрик срабатывает internal mutation checkRules
2. Query загружает активные правила пользователя
3. Для каждого правила проверяются условия
4. При срабатывании вызывается mutation executeAction
```

#### Поток выполнения действия

```
1. Mutation executeAction определяет тип действия
2. Для остановки: Action вызывает VK Ads API (ads.updateAds)
3. Расчёт потенциальной экономии
4. Mutation записывает лог в actionLogs
5. Action отправляет уведомление через Telegram Bot API
6. Дашборд обновляется мгновенно (real-time subscription)
```

### 6.3. Схема базы данных (Convex Schema)

```typescript
// convex/schema.ts
import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

export default defineSchema({
  users: defineTable({
    email: v.string(),
    vkId: v.string(),
    telegramChatId: v.optional(v.string()),
    subscriptionTier: v.union(
      v.literal("freemium"),
      v.literal("start"),
      v.literal("pro")
    ),
    onboardingCompleted: v.boolean(),
    createdAt: v.number(),
  })
    .index("by_vkId", ["vkId"])
    .index("by_email", ["email"]),

  adAccounts: defineTable({
    userId: v.id("users"),
    vkAccountId: v.string(),
    name: v.string(),
    accessToken: v.string(), // encrypted
    refreshToken: v.optional(v.string()),
    status: v.union(
      v.literal("active"),
      v.literal("paused"),
      v.literal("error")
    ),
    lastSyncAt: v.optional(v.number()),
    lastError: v.optional(v.string()),
  })
    .index("by_userId", ["userId"])
    .index("by_vkAccountId", ["vkAccountId"]),

  rules: defineTable({
    userId: v.id("users"),
    name: v.string(),
    type: v.union(
      v.literal("cpl_limit"),
      v.literal("min_ctr"),
      v.literal("fast_spend"),
      v.literal("spend_no_leads")
    ),
    conditions: v.object({
      metric: v.string(),
      operator: v.string(),
      value: v.number(),
      minSamples: v.optional(v.number()),
    }),
    actions: v.object({
      stopAd: v.boolean(),
      notify: v.boolean(),
      notifyChannel: v.optional(v.string()),
    }),
    targetAccountIds: v.array(v.id("adAccounts")),
    targetCampaignIds: v.optional(v.array(v.string())),
    targetAdIds: v.optional(v.array(v.string())),
    isActive: v.boolean(),
    triggerCount: v.number(),
    lastTriggeredAt: v.optional(v.number()),
  })
    .index("by_userId", ["userId"])
    .index("by_userId_active", ["userId", "isActive"]),

  actionLogs: defineTable({
    userId: v.id("users"),
    ruleId: v.id("rules"),
    accountId: v.id("adAccounts"),
    adId: v.string(),
    adName: v.string(),
    actionType: v.union(
      v.literal("stopped"),
      v.literal("notified"),
      v.literal("stopped_and_notified")
    ),
    reason: v.string(),
    metricsSnapshot: v.object({
      cpl: v.optional(v.number()),
      ctr: v.optional(v.number()),
      spent: v.number(),
      leads: v.number(),
    }),
    savedAmount: v.number(),
    status: v.union(
      v.literal("success"),
      v.literal("failed"),
      v.literal("reverted")
    ),
    revertedAt: v.optional(v.number()),
    createdAt: v.number(),
  })
    .index("by_userId", ["userId"])
    .index("by_userId_date", ["userId", "createdAt"])
    .index("by_ruleId", ["ruleId"]),

  metricsDaily: defineTable({
    accountId: v.id("adAccounts"),
    adId: v.string(),
    date: v.string(), // "2026-01-20"
    impressions: v.number(),
    clicks: v.number(),
    spent: v.number(),
    leads: v.number(),
    cpl: v.optional(v.number()),
    ctr: v.optional(v.number()),
  })
    .index("by_accountId_date", ["accountId", "date"])
    .index("by_adId_date", ["adId", "date"]),

  metricsRealtime: defineTable({
    accountId: v.id("adAccounts"),
    adId: v.string(),
    timestamp: v.number(),
    spent: v.number(),
    leads: v.number(),
    impressions: v.number(),
    clicks: v.number(),
  })
    .index("by_adId", ["adId"])
    .index("by_accountId_timestamp", ["accountId", "timestamp"]),
});
```

### 6.4. Интеграция с VK Ads API

#### Используемые методы

| Метод | Назначение | Реализация в Convex |
|-------|------------|---------------------|
| ads.getAccounts | Получение списка кабинетов | Action (при подключении) |
| ads.getCampaigns | Получение кампаний | Action (cron каждые 30 мин) |
| ads.getAds | Получение объявлений | Action (cron каждые 30 мин) |
| ads.getStatistics | Получение метрик | Action (cron каждые 5 мин) |
| ads.updateAds | Остановка объявления | Action (по событию) |

#### Пример Convex Action для VK API

```typescript
// convex/vkApi.ts
import { action } from "./_generated/server";
import { v } from "convex/values";

export const getStatistics = action({
  args: {
    accountId: v.id("adAccounts"),
    adIds: v.array(v.string()),
  },
  handler: async (ctx, args) => {
    const account = await ctx.runQuery(internal.adAccounts.get, {
      id: args.accountId,
    });
    
    const response = await fetch(
      `https://api.vk.com/method/ads.getStatistics?` +
      `account_id=${account.vkAccountId}&` +
      `ids=${args.adIds.join(",")}&` +
      `period=day&date_from=today&date_to=today&` +
      `access_token=${account.accessToken}&v=5.131`
    );
    
    const data = await response.json();
    
    // Сохраняем метрики через mutation
    await ctx.runMutation(internal.metrics.saveRealtime, {
      accountId: args.accountId,
      metrics: data.response,
    });
    
    // Запускаем проверку правил
    await ctx.runMutation(internal.ruleEngine.checkRules, {
      accountId: args.accountId,
    });
    
    return data.response;
  },
});
```

### 6.5. Scheduled Jobs (Crons)

```typescript
// convex/crons.ts
import { cronJobs } from "convex/server";
import { internal } from "./_generated/api";

const crons = cronJobs();

// Синхронизация метрик каждые 5 минут
crons.interval(
  "sync-metrics",
  { minutes: 5 },
  internal.metrics.syncAllActiveAccounts
);

// Синхронизация структуры кампаний каждые 30 минут
crons.interval(
  "sync-campaigns",
  { minutes: 30 },
  internal.adAccounts.syncCampaignStructure
);

// Ежедневный дайджест в 09:00 MSK
crons.daily(
  "daily-digest",
  { hourUTC: 6, minuteUTC: 0 }, // 09:00 MSK
  internal.notifications.sendDailyDigests
);

// Очистка старых realtime-метрик (старше 24 часов)
crons.daily(
  "cleanup-realtime-metrics",
  { hourUTC: 3, minuteUTC: 0 },
  internal.metrics.cleanupOldRealtime
);

export default crons;
```

---

## 7. Технические требования

### 7.1. Технологический стек

| Компонент | Технология | Обоснование |
|-----------|------------|-------------|
| **Frontend** | React 18+ (Vite) | Быстрая разработка, современный tooling |
| **UI Kit** | Shadcn UI + Tailwind | Быстрая разработка профессионального UI |
| **Routing** | React Router / TanStack Router | SPA-навигация |
| **Rich Text** | Tiptap (OSS) | Кастомные отчёты |
| **Backend** | Convex | Serverless backend, real-time из коробки |
| **Database** | Convex Database | Реактивная БД, автоматическое кэширование |
| **Scheduled Jobs** | Convex Cron / Scheduled Functions | Синхронизация метрик каждые 5 мин |
| **Background Tasks** | Convex Actions | HTTP-вызовы к VK API, Telegram |
| **AI** | Convex + OpenAI | Парсинг текста в правила |
| **Telegram** | Convex HTTP Actions | Webhook-обработка, отправка сообщений |
| **File Storage** | Convex File Storage | Скриншоты объявлений, отчёты |
| **Hosting** | Vercel / Netlify | Статический хостинг React-приложения |

### 7.2. Почему Convex?

| Преимущество | Описание |
|--------------|----------|
| **Real-time из коробки** | Дашборд обновляется мгновенно при срабатывании правил |
| **Единый язык** | TypeScript на frontend и backend, полная типизация |
| **Без инфраструктуры** | Не нужно управлять PostgreSQL, Redis, очередями |
| **Транзакции ACID** | Надёжность операций с правилами и логами |
| **Автоматическое кэширование** | Оптимизация запросов без ручной настройки |
| **Scheduled Functions** | Встроенный cron для периодических задач |
| **Горизонтальное масштабирование** | Автоматическое, без настройки |

### 7.3. Структура Convex-функций

```
convex/
├── schema.ts              # Схема базы данных
├── auth.ts                # Аутентификация (VK OAuth)
├── users.ts               # Управление пользователями
├── adAccounts.ts          # Рекламные кабинеты
├── rules.ts               # CRUD правил
├── ruleEngine.ts          # Логика проверки правил
├── metrics.ts             # Метрики и аналитика
├── actionLogs.ts          # Логи действий
├── notifications.ts       # Отправка уведомлений
├── vkApi.ts               # HTTP Actions для VK Ads API
├── telegram.ts            # HTTP Actions для Telegram Bot
└── crons.ts               # Scheduled jobs (синхронизация)
```

### 7.4. Нефункциональные требования

#### Производительность

| Метрика | Требование | Как достигается в Convex |
|---------|------------|--------------------------|
| Загрузка дашборда | < 2 секунд | Автоматическое кэширование queries |
| Время реакции правила | < 60 секунд | Cron каждые 5 мин + real-time updates |
| Real-time обновления | < 100 мс | WebSocket subscriptions из коробки |
| Пропускная способность | 1000 кабинетов за 5-минутный цикл | Параллельные Actions |

#### Надёжность

| Метрика | Требование | Как достигается в Convex |
|---------|------------|--------------------------|
| Uptime | 99.9% | Managed infrastructure Convex |
| ACID транзакции | Гарантированы | Встроено в Convex mutations |
| Бэкапы | Автоматические | Convex managed backups |
| Graceful degradation | При недоступности VK API | Retry logic в Actions |

#### Безопасность

| Требование | Реализация |
|------------|------------|
| Шифрование токенов | Environment variables в Convex |
| HTTPS | Автоматически (Convex) |
| Auth | Convex Auth + VK OAuth |
| Row-level security | Проверки в каждой функции |

#### Масштабируемость

| Аспект | Решение в Convex |
|--------|------------------|
| Горизонтальное масштабирование | Автоматическое |
| Индексация | Declarative indexes в schema |
| Долгие операции | Actions с retry |

### 7.5. Пример React-компонента с Convex

```tsx
// src/components/Dashboard.tsx
import { useQuery, useMutation } from "convex/react";
import { api } from "../convex/_generated/api";

export function Dashboard() {
  // Real-time подписка на данные — автоматически обновляется
  const savedToday = useQuery(api.metrics.getSavedToday);
  const recentLogs = useQuery(api.actionLogs.getRecent, { limit: 10 });
  const accounts = useQuery(api.adAccounts.list);

  // Mutation для отката действия
  const revertAction = useMutation(api.actionLogs.revert);

  if (!savedToday || !recentLogs || !accounts) {
    return <LoadingSpinner />;
  }

  return (
    <div className="dashboard">
      {/* Виджет экономии — обновляется real-time */}
      <SavingsWidget amount={savedToday.amount} trend={savedToday.trend} />
      
      {/* Список кабинетов */}
      <AccountsList accounts={accounts} />
      
      {/* Лента событий с возможностью отката */}
      <EventFeed 
        logs={recentLogs} 
        onRevert={(logId) => revertAction({ logId })}
      />
    </div>
  );
}
```

### 7.6. Преимущества Convex для AdPilot

| Традиционный стек | Convex | Выгода |
|-------------------|--------|--------|
| PostgreSQL + Redis + BullMQ | Convex Database + Crons | Меньше инфраструктуры |
| REST API + WebSocket сервер | Queries + Mutations + Subscriptions | Real-time из коробки |
| Ручное кэширование | Автоматическое | Меньше кода |
| Docker + Kubernetes | Managed platform | Быстрее разработка |
| Отдельные типы для FE/BE | Единая типизация | Type safety |

---

## 8. Матрица RACI

| Задача | PM | Tech Lead | Frontend | Backend | Designer | QA | Marketing |
|--------|:--:|:---------:|:--------:|:-------:|:--------:|:--:|:---------:|
| Определение требований | A/R | C | C | C | C | I | C |
| Архитектура системы | A | R | C | C | I | I | - |
| UI/UX дизайн | A | C | C | I | R | I | C |
| Frontend разработка | I | A | R | C | C | C | - |
| Backend разработка | I | A | C | R | - | C | - |
| Интеграция VK Ads API | I | A | - | R | - | C | - |
| Telegram бот | I | A | - | R | - | C | - |
| Тестирование | I | A | C | C | - | R | - |
| Бета-запуск | A/R | C | C | C | - | R | R |
| Маркетинг и PR | A | I | - | - | C | - | R |

**Легенда:** R = Responsible, A = Accountable, C = Consulted, I = Informed

---

## 9. Дорожная карта

### Фаза 1: Beta (8 недель)

#### Недели 1-2: Инфраструктура
- [ ] Настройка Convex проекта и schema
- [ ] Интеграция VK OAuth (Convex Auth)
- [ ] Базовые Convex functions (users, adAccounts)
- [ ] Настройка Vercel для React-приложения

#### Недели 3-4: Core Backend (Convex)
- [ ] Actions для VK Ads API интеграции
- [ ] Cron для синхронизации метрик (каждые 5 мин)
- [ ] Rule Engine (mutations + queries)
- [ ] Правило «Быстрый скрут»

#### Неделя 5: Telegram Bot
- [ ] HTTP Action для Telegram Bot API
- [ ] Webhook endpoint для входящих сообщений
- [ ] Базовые команды (/start, /status)
- [ ] Уведомления о срабатывании правил

#### Недели 6-7: Frontend (React)
- [ ] Дашборд с real-time виджетом экономии
- [ ] Список кабинетов (useQuery)
- [ ] Список правил + редактор
- [ ] Экран логов с фильтрацией

#### Неделя 8: Beta Launch
- [ ] Приглашение 20-30 таргетологов-инфлюенсеров
- [ ] Сбор обратной связи
- [ ] Исправление критичных багов

### Фаза 2: MVP Launch (4 недели)

- [ ] Система подписок и оплаты
- [ ] Полный набор типов правил (CPL, CTR, Сумма без лидов)
- [ ] Экран аналитики
- [ ] Публикация кейса на VC.ru
- [ ] Запуск рекламной кампании

### Фаза 3: Scaling (8+ недель)

- [ ] Реферальная программа для агентств
- [ ] Поддержка форматов VK Clips, Mini Apps
- [ ] AI-рекомендации по оптимизации
- [ ] White-label для агентств

---

## 10. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Задержки VK Ads API | Высокая | Среднее | Кэширование, предиктивные алгоритмы |
| Недоверие пользователей к автоматике | Средняя | Высокое | Режим «Только уведомления», детальные логи |
| VK запускает аналогичный функционал | Средняя | Высокое | Фокус на гибкости, мульти-аккаунтах, UX |
| Изменения в API VK | Низкая | Высокое | Модульная архитектура, слой абстракции API |
| Демпинг конкурентов | Низкая | Среднее | Акцент на ценности (экономия), а не цене |

---

## 11. Метрики успеха

### Go/No-Go критерии (MVP Launch)

| Метрика | Цель | Критично |
|---------|------|----------|
| Retention Rate (1 месяц) | > 40% | ✅ |
| Снижение CPL (тестовая группа) | > 15% | ✅ |
| Конверсия в платную подписку | > 5% | ✅ |
| Uptime за период beta | > 99% | ✅ |
| NPS | > 30 | ❌ |

### Бизнес-метрики (Post-Launch)

| Метрика | Цель (6 месяцев) |
|---------|------------------|
| MRR | 300,000 ₽ |
| CAC | ≤ 800 ₽ |
| LTV/CAC | > 3 |
| Churn Rate | < 7% / месяц |

---

## 12. Приложения

### Глоссарий

| Термин | Определение |
|--------|-------------|
| CPL | Cost Per Lead — стоимость одного лида |
| CTR | Click-Through Rate — коэффициент кликабельности |
| Быстрый скрут | Аномально быстрый расход бюджета за короткий период |
| Открутка | Процесс расхода рекламного бюджета |
| Convex Query | Функция для чтения данных с real-time подпиской |
| Convex Mutation | Функция для изменения данных в БД |
| Convex Action | Функция для HTTP-запросов и side effects |
| Convex Cron | Scheduled job для периодических задач |

### Ссылки на документацию

- [VK Ads API Documentation](https://dev.vk.com/ru/api/ads/getting-started)
- [Convex Documentation](https://docs.convex.dev/)
- [Convex + React Guide](https://docs.convex.dev/client/react)
- [Convex Scheduled Functions](https://docs.convex.dev/scheduling/cron-jobs)
- [React Documentation](https://react.dev/)
- [Shadcn UI](https://ui.shadcn.com/)

### Контакты

По вопросам по данному PRD обращайтесь к Product Manager.

---

*Документ подготовлен: 20 января 2026 г.*  
*Следующий review: 27 января 2026 г.*
